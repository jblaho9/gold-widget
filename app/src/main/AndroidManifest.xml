<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_gold"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Simple price widget -->
        <receiver
            android:name=".SimpleGoldWidget"
            android:exported="true"
            android:label="@string/simple_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_simple_info" />
        </receiver>

        <!-- Detailed widget with highs/lows -->
        <receiver
            android:name=".DetailedGoldWidget"
            android:exported="true"
            android:label="@string/detailed_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_detailed_info" />
        </receiver>

        <!-- Refresh widget on phone unlock -->
        <receiver
            android:name=".ScreenUnlockReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>

        <!-- Lightweight service for on-demand refresh (bypasses WorkManager overhead) -->
        <service
            android:name=".RefreshService"
            android:exported="false" />

    </application>

</manifest>
